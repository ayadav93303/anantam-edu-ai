const navs=document.querySelectorAll('.nav');
function show(id){document.querySelectorAll('.screen').forEach(x=>x.classList.remove('active'));document.getElementById(id).classList.add('active');navs.forEach(n=>n.classList.toggle('active',n.dataset.id===id));scrollTo({top:0,behavior:'smooth'});}
navs.forEach(n=>n.onclick=()=>show(n.dataset.id));
function openChat(){show('chat');setTimeout(()=>document.getElementById('chatInput').focus(),80)}
function chatWith(t){show('chat');setTimeout(()=>{document.getElementById('chatInput').value=t;document.getElementById('chatForm').dispatchEvent(new Event('submit',{bubbles:true,cancelable:true}))},100)}
function addMsg(text,user=false){let box=document.getElementById('messages'),d=document.createElement('div');d.className='message '+(user?'user':'ai');d.innerHTML=user?`<div class="bubble">${esc(text)}</div>`:`<img src="assets/icon.svg"><div class="bubble"><b>Anantam Edu AI</b><p>${text}</p></div>`;box.appendChild(d);box.scrollTop=box.scrollHeight}
function esc(s){return s.replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}
async function askBackend(prompt){
  document.getElementById('aiStatus').textContent='● Thinking…';
  try{let r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:prompt,userName:'Amit'})});if(!r.ok)throw Error();let j=await r.json();return j.reply}
  catch(e){return localReply(prompt)}
  finally{document.getElementById('aiStatus').textContent='● Ready'}
}
function localReply(q){let x=q.toLowerCase();if(x.includes('photosynthesis'))return'Photosynthesis is the process in which green plants use sunlight, water and carbon dioxide to make food. Oxygen is released as a by-product.';if(x.includes('math')||x.includes('solve'))return'Send the complete question and I will break the solution into clear steps.';return'I can help you explain topics, solve homework, create notes and prepare exam papers. For full AI answers, connect the included backend to an AI provider.'}
document.getElementById('chatForm').onsubmit=async e=>{e.preventDefault();let i=document.getElementById('chatInput'),q=i.value.trim();if(!q)return;addMsg(q,true);i.value='';addMsg(await askBackend(q))}
async function generateNotes(){let topic=document.getElementById('noteTopic').value,cl=document.getElementById('noteClass').value,sub=document.getElementById('noteSubject').value,lang=document.getElementById('noteLang').value;let out=document.getElementById('noteOutput');out.innerHTML='<div class="note-card">Generating your notes…</div>';try{let r=await fetch('/api/notes',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({topic,className:cl,subject:sub,language:lang,userName:'Amit'})});if(!r.ok)throw Error();let j=await r.json();out.innerHTML=`<div class="note-card"><h3>${esc(topic)} — ${esc(cl)}</h3>${j.html||'<p>'+esc(j.text)+'</p>'}</div>`}catch(e){out.innerHTML=`<div class="note-card"><h3>${esc(topic)} — ${esc(cl)}</h3><ul><li>Definition and core idea</li><li>Important terms and keywords</li><li>Main process / formula / examples</li><li>Common exam points</li><li>Quick revision summary</li></ul><p><b>AI backend:</b> add the backend environment variables described in README.md for generated notes.</p></div>`}}
async function generateExam(){let data={className:examClass.value,subject:examSubject.value,marks:marks.value,difficulty:difficulty.value,topics:examTopics.value,mix:mix.value,userName:'Amit'};let out=document.getElementById('examOutput');out.innerHTML='<div class="note-card">Preparing your question paper…</div>';try{let r=await fetch('/api/exam',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(!r.ok)throw Error();let j=await r.json();out.innerHTML=j.html}catch(e){out.innerHTML=demoExam(data)}}
function demoExam(d){return `<div class="paper"><div class="paper-head"><img src="assets/icon.svg"><h2>ANANTAM EDU AI</h2><p>${esc(d.className)} • ${esc(d.subject)}</p><p>EXAM PREPARATION QUESTION PAPER • ${esc(d.marks)} MARKS</p></div><h3>Section A — MCQ</h3><div class="q">1. Choose the correct answer. (5 × 1 = 5)</div><div class="q">2. Which concept is most closely related to ${esc(d.topics||'the chapter')}?</div><div class="q">3. Write one important definition from the chapter.</div><h3>Section B — Short Answer</h3><div class="q">4. Explain the main concept in your own words.</div><div class="q">5. Give two examples and explain them.</div><h3>Section C — Long Answer</h3><div class="q">6. Answer a detailed question covering the key ideas of the chapter.</div><p><b>Tip:</b> Use the AI backend for a full paper tailored to the selected class, marks and chapters.</p></div>`}
function pick(b,ok){document.querySelectorAll('.answers button').forEach(x=>x.disabled=true);b.classList.add(ok?'good':'bad');document.getElementById('result').textContent=ok?'✓ Correct!':'✗ Try again next time.'}
document.getElementById('themeBtn').onclick=()=>{document.body.classList.toggle('light');document.getElementById('themeBtn').textContent=document.body.classList.contains('light')?'☀':'☾'}
